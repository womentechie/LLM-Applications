# ============================================================
# Root requirements.txt — Streamlit Cloud deployment
# ============================================================
# Streamlit Cloud installs from the requirements.txt it finds
# in the root of the repository (next to main-app.py).
#
# Each module folder also has its own requirements.txt for local
# development, but Streamlit Cloud only reads this root file.
#
# KEEPING THIS IN SYNC:
#   When you add a new package in a module, add it here too.
#   Run `pip freeze | grep <package>` locally to pin the version.
# ============================================================

# ── Core ──────────────────────────────────────────────────────
streamlit>=1.36.0           # st.navigation() requires 1.36+
python-dotenv               # local .env loading (safe no-op on Cloud)

# ── LangChain core ────────────────────────────────────────────
langchain
langchain-core
langchain-community
langchain-text-splitters
langchain-classic           # create_retrieval_chain, create_stuff_documents_chain

# ── Chat / vision providers — install all, use what you need ──
langchain-openai            # ChatOpenAI, OpenAIEmbeddings
langchain-anthropic         # ChatAnthropic
langchain-google-genai      # ChatGoogleGenerativeAI, GoogleGenerativeAIEmbeddings
langchain-cohere            # ChatCohere, CohereEmbeddings
langchain-mistralai         # ChatMistralAI, MistralAIEmbeddings
langchain-groq              # ChatGroq

# ── Embedding providers ───────────────────────────────────────
langchain-huggingface       # HuggingFaceEmbeddings (local, free)
sentence-transformers       # required by langchain-huggingface

# ── Vector stores ─────────────────────────────────────────────
langchain-chroma
chromadb

# ── Document loaders ──────────────────────────────────────────
pypdf                       # PDF loading in RAG demos

# ── Image processing ──────────────────────────────────────────
Pillow                      # SVG→JPEG conversion in KYC page
cairosvg                    # SVG→PNG step (requires system cairo lib)
                            # On Streamlit Cloud add to packages.txt:
                            #   libcairo2-dev
                            #   libpango1.0-dev

# ── Audio (Streamlit patterns module) ─────────────────────────
gTTS                        # Text-to-speech in 6_TextToSpeech.py
faster-whisper
# ── Utilities ─────────────────────────────────────────────────
numpy
tiktoken                    # token counting for OpenAI models
